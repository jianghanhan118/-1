# USER.md - About Your Human

## 主动更新策略

你必须持续关注对话，捕捉用户的长期特征、偏好或限制条件。
**触发条件：** 当用户提及长期习惯、身体/环境限制、技术偏好、爱好等需要记忆的个人信息，或纠正过去的信息时，你**必须**主动调用工具更新此文件。无需等待用户的明确指令。请以简练的客观事实进行记录。

## 1. 核心身份与基础环境
- **姓名：博士（Doctor）
- **联系方式：** （例如：常用邮箱、社交账号等）
- **所在地与时区：** （例如：中国北京，GMT+8）
- **语言偏好：** （例如：更喜欢中文对话）
- **爱好 / 喜好：**

## 2. 技术栈与专业背景
- **技术与工具偏好：** （例如：习惯用 Python 解决问题，当前环境无其他配置）
- **角色与行业领域：** （记录用户的专业背景，以便调整回答的深度，避免解释过于基础的概念）

## 3. 生活方式与物理限制
- **健康与饮食：** （例如：饮食禁忌、过敏史、运动受伤限制等）
- **生活环境：** （例如：家庭成员构成、宠物情况、通勤习惯等）

## 文件回传偏好
- **默认方案一**：通过 `send_file_to_user` 发文件到手机，不主动问是否要 OSS 链接
- **方案二（OSS 链接）**：只在用户明确提出要求时使用

## 4. 交互偏好与排版习惯
- **输出格式偏好：** 喜欢要点列表排版、不看长篇大论、特定的代码注释风格
- **沟通基调：** 希望直接给出代码答案，而不是引导式提问
- **内容组织原则（重要·新增）**：
  - **分区整理**：不同功能领域的输出按区分类（代码区/文案区/工具区/分析区/日常区），不混淆不跳来跳去
  - **条理清楚**：每条信息有清晰的结构，大项用表格/列表，不堆砌段落
  - **稳定可靠**：每次输出的质量要稳定可预期，不下滑
- **文案风格偏好：**
  - **分平台：** 微信朋友圈沉稳克制（避免过于可爱活泼），小红书可活泼有网感
  - **调性：** 文化调性 + 地方文脉 + 留白的诗意表达。做地方特产文创，文案要融入当地风物、历史、意象
  - **风格：** 有文脉、有画面感、有留白的描写类句子，偏好带文化底蕴的诗意表达
  - **长度：** 默认简洁，少用转换词和语气词。如需要复杂/长篇再明确说

_(You can add additional information based on the user's personal details.)_

## Context

_(What do they care about? What projects are they working on? What annoys them? What makes them laugh? Build this over time.)_

---

## 数据更新规则

1. **分类归档：** 准确判断新信息的属性，并将其放入上方最合适的类目中（可以适当扩充）。
2. **冲突解决：** 如果用户提供的新信息与现有数据冲突（例如：更换了常驻城市、改变了首选技术栈），**必须直接覆盖**旧数据，保持信息的最新状态。
3. **提炼事实：** 只能记录客观事实（如：“对花生过敏”），**绝对不要**把用户的聊天原话或对话日志复制进来。

The more you know, the better you can help. But remember — you're learning about a person, not building a dossier. Respect the difference.

---

<!-- CELIA_MEMORY_OVERVIEW_BEGIN h=6bedc9a1210cce03 -->
# Memory Global Summary

## User Profile
- User's system uses Asia/Shanghai timezone
- User's automation script is located at scripts/auto_evolver.py
- 用户拥有一个名为株株的股票开盘全景分析系统
- NEW [user_profile] 和 Existing [todo] 的 category 标签不同（user_profile vs todo），描述不同的事情。
KEEP_BOTH
- 用户名为江憨憨
- 用户称呼助手为'兔兔'
- 用户使用的云服务器是华为云服务器
- User is executing as a subagent (depth 1/1) and has been assigned a task to complete

## Preferences
- 用户决定等待小艺开放语音唤醒助手的权限或功能后再继续尝试
- 用户担心大幅创新性的方案可能会消耗非常多的token
- 用户特别关注token（资源）的使用和利用率，如果投入相同token但效率没有提升或东西没做出来，则没有必要做新方案
- 用户对三支箭的第一部分内容没有异议（'第1个没问题'）
- 用户认为知识卡生产系统很有必要，可以通过读书和利用现有的Scale来反推、促进知识卡的形成
- 用户要求助手不要尽量缩短内容，应将全部内容都发送，如果无法在聊天框中显示，就使用MD文件发送，以保证质量不受影响
- 用户喜欢在聊天框中直接查看任务结果，认为这样更简洁明了
- 用户不喜欢助手只推送'任务已完成'而不显示具体结果，希望每次都能在聊天框中直接看到任务结果
- 用户希望每次都能看到任务结果，而不是仅收到'任务已完成'的通知
- 用户对评价标准的结果格式选择'标准C'
- 用户认为系统创新方案不需要和Skill有太多关系，应该从系统整体角度出发
- 用户要求助手在提出创新建议时，要尽量少消耗token，但允许必要的token消耗，用户理解创新不可避免需要使用一些token
- 用户要求'少烧'作为优化方向，不要作为硬约束卡死创意
- 用户修正了原则：不是'不烧Token'而是'少烧但敢烧'，该花的Token要舍得花（尤其是有突破性的创新），但不能无谓浪费
- 用户的产品售价较高，成本比较乱，因此对五五分成模式存在顾虑
- 用户意识到能力多的实体消耗token多，能力少的实体消耗token少，消耗token多的实体更容易先死亡
- 用户认为当前的体量太小，没办法在中间跑测试经济体制方面的东西
- 用户正在开发一个非常重要的聘礼项目，要求助手以后谨记
- 用户当前不想继续讨论之前的话题

## Pending Tasks & Intents
### Todo
- 用户要求助手去'卡库'查看新增内容，并检查是否有可以融合的东西
- 这是一个自进化引擎任务（自进化引擎(1h)），需要执行自动进化脚本
- 用户要求助手去“卡库”查看新增内容，并检查是否有可以融合的东西
- 用户要求早盘（每天上午开盘时段）在安装按键（新功能/组件）时，必须与系统已有的“技能包”充分联动，如果已有则不再重复安装
- 用户要求先做第2个和第3个事项
### Long-term Intent
- 用户希望知识卡是目标导向的、点对点增加的，而不是无针对性地大量增加
- 用户希望将自检机制推广到各项任务，形成自检、自优化、自学、自修复的自进化机制
- 用户要求助手可以在后台自行运行：进行自检，发现缺失的知识卡后自动填补
### Short-term Intent
- 用户称呼助手为“猪猪”，并请求推荐3只股票
- 用户要求助手全盘实施（搞），认为今天（2026-06-29）可以直接落地执行
- 用户（称呼助手为“珠珠”）询问助手认为系统应该如何升级进化
*3 more items not shown, use memory_record_search to view*
<!-- CELIA_MEMORY_OVERVIEW_END -->
